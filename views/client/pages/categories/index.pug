extends ../../layouts/default.pug
include ../../mixins/box-head.pug

block main
    .container.my-3
        .row
            .col-12
                +box-head(pageTitle)

        .row
            each category in categories
                .col-6.mb-3
                    div(class="card")
                        img(src=category.image)
                        div(class="card-body")
                            h5(class="card-title") #{category.title}
                            a(href=`/tours/${category.slug}` class="btn btn-primary") Xem chi tiet
                